print("apartamento:")

res = int(input())

lado = "abacaxi"

if res % 2 == 0:
   lado = "esquerda"
else:
    lado = "direita"
    
texto = """
Para ir ao apartamento {}, vire à {}.
""".format (res, lado)

print(texto)
